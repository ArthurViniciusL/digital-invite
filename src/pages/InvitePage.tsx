import { PageWrapper } from '@/components/layout/PageWrapper'

/**
 * Public invite page, served at `/`.
 *
 * TODO: compose `InviteHero`, `EventDetails` and `RsvpForm` once they exist.
 */
export function InvitePage() {
  return <PageWrapper>Invite Page — under construction</PageWrapper>
}
