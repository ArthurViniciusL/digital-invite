/**
 * Public RSVP form: name, email and guest count.
 *
 * TODO: implement with react-hook-form wired to `rsvpSchema` through
 * `@hookform/resolvers/zod`, submitting an insert into the Supabase `rsvp`
 * table with the anon key.
 */
export function RsvpForm() {
  return <form>RSVP form — under construction</form>
}
