/**
 * Dashboard table listing every confirmation received.
 *
 * TODO: implement with the columns Name, Email, Guest count and Status, fed by
 * the `useRsvpList` hook.
 */
export function RsvpTable() {
  return <table>RSVP table — under construction</table>
}
