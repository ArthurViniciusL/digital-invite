/**
 * Administrator sign-in form: email and password.
 *
 * TODO: implement with `supabase.auth.signInWithPassword`, showing credential
 * errors inline and redirecting to `/admin` on success. There is no public
 * sign-up flow: the single admin user is created by hand in the Supabase
 * dashboard.
 */
export function LoginForm() {
  return <form>Login form — under construction</form>
}
